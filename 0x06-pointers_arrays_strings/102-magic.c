 #include "main.h"

/**
 * main - This is the code pattern for array
 * Remember: -you are not allowed to use a, and you are not allowed to modify p
 * -only one statement and
 *  -you are not allowed to code anything else than this line of code
 *  main: Don't know why betty keeps asking another main description
 */

int main(void)
{
	int n;
	int a[5];
	int *p;

	a[2] = 1024;
		p = &n;
			p = &n;
			/*...so that this prints 98\n */
			printf("a[2] = %d\n", a[2]);

			return (0);
}
